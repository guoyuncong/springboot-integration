server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8
    # 最大工作线程数 默认200
    max-thread: 800
    # 最大连接数
    max-connections: 2000
    # 最小工作空闲线程数 默认数10
    min-spare-threads: 30
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring-security?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: yagena*2020&2019
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.springboot.security.model.entity

token:
  header: Authorization
  secret: abcdefghijklmnopqrstuvwxyz
  expireTime: 30



