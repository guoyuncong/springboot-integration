<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.druid.mapper.ShipMapper">


    f<select id="list" resultType="com.springboot.druid.model.entity.Ship" >
        select * from ship where ship_id = #{id, jdbcType=VARCHAR};
    </select>
</mapper>